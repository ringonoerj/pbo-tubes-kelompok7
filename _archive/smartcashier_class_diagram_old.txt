@startuml
left to right direction
skinparam classAttributeIconSize 0
title Class Diagram - SmartCashier

' === Abstract Class ===
abstract class User {
  # id : String
  # name : String
  # phoneNumber : String
  + {abstract} displayProfile() : void
  + getId() : String
}

' === Karyawan ===
class Karyawan {
  - employeeId : String
  - password : String
  - kpiScore : double
  + login(id : String, pass : String) : boolean
  + getKpi() : double
  + displayProfile() : void
}
Karyawan -up-|> User

' === Member ===
class Member {
  - joinDate : String
  - discountRate : double
  + applyDiscount(totalBelanja : double) : double
  + displayProfile() : void
}
Member -up-|> User

' === Product ===
class Product {
  - productName : String
  - category : String
  - quantity : int
  - price : double
  + getDetails() : String
  + updateQuantity(qty : int) : void
}

' === Transaction ===
class Transaction {
  - transactionId : String
  - cashier : Karyawan
  - member : Member
  - items : List<Product>
  - totalAmount : double
  + addItem(p : Product) : void
  + calculateTotal() : double
  + printReceipt() : void
}

' === CalculatorTools ===
class CalculatorTools {
  + {static} add(a : double, b : double) : double
  + {static} subtract(a : double, b : double) : double
  + {static} multiply(a : double, b : double) : double
  + {static} divide(a : double, b : double) : double
  + {static} modulo(a : double, b : double) : double
}

' === Relasi ===
Transaction o-- Product
Transaction --> Karyawan
Transaction --> Member
Transaction ..> CalculatorTools : <<use>>
@enduml